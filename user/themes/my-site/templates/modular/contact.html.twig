{# <section class="contact home-section">
	<div class="bg-circle"></div>
	<div class="bottom-title">{{page.header.title}}</div>
    <div class="home-section__content">
        <div class="home-section__content--inner">
			<div class="vert-text">{{page.header.title}}</div>
	        {{ page.content }}
            <div class="eyebrow">Email me</div>
            {% for email in page.header.email %}
                    <a class="email-link" href="{{ email.link }}">
                        {{ email.text }}
                    </a>
            {% endfor %}
            <div class="eyebrow">Find me on</div>
            {% for item in page.header.social %}
                    <a class="social-link" href="{{ item.link }}" style="color:{{item.color}};">
                        {{ item.text }}
                    </a>
            {% endfor %}
            
        </div>
    </div>
</section> #}
    
{% extends 'contact.html.twig' %}